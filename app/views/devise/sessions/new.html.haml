.login__account-page
  .login__account-page__inner.clearfix
    .login__account-page__inner--left.login__account-page__header-login
      %h2.letsLogin Let's login
      %h5 さあ、あなたの好きなボードを作って
      %h5 共通の話題を仲間でチャットしましょう
    .login__account-page__inner--right.user-form
      = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        .field
          .field-label
            = f.label :name
          .field-input
            = f.text_field :name, autofocus: true
          .field-label
            = f.label :email
          .field-input
            = f.email_field :email, autofocus: true
          .field
            .field-label
              = f.label :password
              %i (英数字8文字以上)
            .field-input
              = f.password_field :password, autocomplete: "off"
          .actions
            = f.submit "Log in", class: 'btn'
